# Captain OSS scout — 2026-09-02

Scope: fresh daily scan only. No third-party code was installed or executed. Previously recorded candidates were not re-added unless there was a materially new signal.

## schmitthub/clawker
- Capability: local, harness-agnostic coding-agent sandboxes in Docker with project namespaces, git-worktree lifecycle, deny-by-default egress controls, audit/observability, OAuth callback forwarding and a host control plane.
- Maintenance/activity: public repository created 2026-01-07; pushed 2026-08-31; repository metadata observed 2026-09-02 showed active issues/discussions.
- License: AGPL-3.0.
- Captain fit: strong security/egress and agent-isolation reference, particularly for a future optional sandbox adapter. Its own host control plane overlaps Captain's single-control-plane requirement, and AGPL plus Docker/resource cost make direct embedding unattractive.
- Decision: architecture/reference only for now. If evaluated later, keep Captain authoritative and adapt only the sandbox boundary; default off, explicit Settings permission, no secret copying.

## Handler.dev
- Capability: human-in-the-loop command centre for multiple coding agents with isolated Docker sandboxes, Firecracker on Linux/WSL, terminals, metrics, workspace forking and MCP configuration.
- License signal: project site states MIT and self-hostable.
- Captain fit: useful UI reference for the interactive builder pane (parallel sessions, terminal/metrics, review/fork flow) and potential sandbox adapter on WSL/Linux.
- Cost/risk: Docker/Firecracker is heavier than Captain's laptop-first baseline; do not introduce Handler as a second orchestrator/control plane.
- Decision: study UI/session patterns and adapter boundaries; no install.

## Content-addressed candidate handoff pattern
- Fresh GitHub reference: EffortlessMetrics/perl-lsp-swarm PR #14535 describes a self-contained, independently re-verifiable candidate envelope with isolated object-database validation, exact object-closure checking, staged validation before atomic publication, explicit repository-identity uncertainty and credential refusal.
- Captain fit: directly relevant to GitHub fallback -> laptop reconciliation. Captain does not need to copy that implementation; the useful design principle is to bind a parked candidate to independently re-checkable repository/head/tree/base/content identity and never let transport metadata imply publication or local verification.
- Decision: adopted only as a design principle in Captain's new small parking `candidate_handoff` reference contract; no third-party code copied or executed.

## Community signals
- Recent Reddit discussions around Contained Pods and Isolade again emphasize that worktrees alone do not isolate filesystem/network/runtime effects. Rootless containers or microVMs, network allowlists, disposable environments, and explicit host-folder mounts are recurring practical controls.
- These are experience reports, not security proofs. They support Captain's existing direction: worktree scope + execution sandbox + explicit network/credential policy + validation/promotion gates.

## Priority
1. Keep Captain's single router/control-plane and fail-closed chat/project/repo walls.
2. On reconnect, locally reconcile the content-addressed handoff contract with the real Captain workspace and full Doctor/router/isolation acceptance suite.
3. For a future sandbox subsystem, evaluate a thin adapter boundary before choosing an implementation; Clawker/Handler are references, not default dependencies.
